#lang racket

(require 2htdp/image)

(define-struct posn (x y))

(define debug #t)

(define (drawBackground w img)
  img
)

(define background (rectangle 768 432 "solid" "white"))

(drawBackground 0 background)